# 🥫 Vending Machine Simulation (Java OOP Project)

โปรเจกต์นี้คือโปรแกรมจำลอง **ตู้ขายของอัตโนมัติ** ที่สร้างด้วยภาษา Java โดยมุ่งเน้นการออกแบบเชิงวัตถุ (Object-Oriented Programming - OOP) ที่ถูกต้องและเป็นสัดส่วน

ผู้ใช้สามารถโต้ตอบกับตู้ผ่าน Console เพื่อเลือกสินค้า, หยิบใส่ตะกร้า, เลือกวิธีชำระเงิน, รับสินค้า และรับเงินทอน นอกจากนี้ยังมี "โหมดแอดมิน" ที่ซ่อนไว้สำหรับจัดการสต็อกและเก็บเงินอีกด้วย

## ✨ ฟีเจอร์หลัก (Features)

  * **แสดงรายการสินค้า:** แสดงสินค้าทั้งหมดในตู้ พร้อมราคาและจำนวนคงเหลือ
  * **ตะกร้าสินค้า (Shopping Cart):** ผู้ใช้สามารถเลือกสินค้าหลายชิ้นใส่ตะกร้าก่อนชำระเงิน
  * **ชำระเงิน (Polymorphism):** รองรับการจ่ายเงินหลายรูปแบบ (Strategy Pattern):
      * `💳` **QR Code** (จำลองการจ่ายสำเร็จ)
      * `🪙` **เหรียญ** (รับค่า 1, 2, 5, 10)
      * `💵` **ธนบัตร** (รับค่า 20, 50, 100, 500, 1000)
  * **การจัดการเงินทอน:** ระบบจะตรวจสอบเงินทอนในตู้ (Internal Cash) ก่อนรับเงินสด ถ้าเงินทอนไม่พอ จะยกเลิกการซื้อ
  * **ระบบสมาชิก:** สามารถกรอกเบอร์โทรศัพท์เพื่อสะสมแต้มได้ (1 บาท = 1 แต้ม)
  * **จัดการ Error (Exception Handling):** มีการดักจับข้อผิดพลาดอย่างชัดเจน:
      * `❌ OutOfStockException`: ของหมด
      * `❌ InsufficientFundsException`: เงินไม่พอ (หรือผู้ใช้กดยกเลิก)
      * `❌ ChangeNotAvailableException`: ตู้มีเงินทอนไม่พอ

## 🧑‍💼 ฟีเจอร์แอดมิน (Admin Mode)

โปรเจกต์นี้ใช้ `AdminService` เพื่อ **ห่อหุ้ม (Encapsulate)** เมธอดที่อันตรายไว้ ทำให้ `VendingMachine` (หน้าตู้) ปกติไม่สามารถเรียกใช้ได้

  * **เติมของ (Restock):** เติมสินค้าเข้าช่องที่กำหนด
  * **เก็บเงิน (Collect Cash):** นำเงินสดทั้งหมดออกจากลิ้นชัก
  * **ปรับราคา (Set Price):** เปลี่ยนราคาสินค้าในช่อง

-----

## 🏗️ สถาปัตยกรรมและโครงสร้างโปรเจกต์

โปรเจกต์นี้แบ่งความรับผิดชอบ (Separation of Concerns) อย่างชัดเจน โดยใช้โครงสร้างที่คล้ายกับ **Model-View-Controller (MVC)**

  * **View (หน้าจอ):** `VendingMachine.java`
  * **Controller (สมอง):** `VendingMachineController.java`
  * **Model (ตรรกะ/ข้อมูล):** ทุกคลาสใน Package อื่นๆ (Products, Payment, Users)

<!-- end list -->

```
vendingmachine/
├── VendingMachine.java           (View - รับ Input/แสดงผล Console)
├── VendingMachineController.java   (Controller - ตัวกลางประสานงาน)
│
├── admin/
│   └── AdminService.java           (Service - รวมเมธอดสำหรับแอดมิน)
│
├── exceptions/
│   ├── VendingMachineException.java  (คลาสแม่ของ Error)
│   ├── ChangeNotAvailableException.java
│   ├── InsufficientFundsException.java
│   └── OutOfStockException.java
│
├── payment/
│   ├── PaymentMethod.java          (Interface - ข้อตกลงการจ่ายเงิน)
│   ├── CoinReceiver.java         (Concrete - รับเหรียญ)
│   ├── BanknoteReceiver.java     (Concrete - รับแบงค์)
│   ├── QRReceiver.java           (Concrete - รับ QR)
│   ├── MoneyManager.java         (Service - จัดการเงินทอน, เลือกวิธีจ่าย)
│   └── PaymentResult.java        (Data - ผลลัพธ์การจ่ายเงิน)
│
├── products/
│   ├── Product.java              (Abstract - คลาสแม่สินค้า)
│   ├── Drink.java                (Concrete - สินค้าเครื่องดื่ม)
│   ├── Snack.java                (Concrete - สินค้าขนม)
│   ├── ItemSlot.java             (Model - ช่องวางสินค้า 1 ช่อง)
│   └── InventoryManager.java     (Service - จัดการคลังสินค้า/สต็อก)
│
└── users/
    ├── Member.java               (Model - ข้อมูลสมาชิก)
    └── MemberDatabase.java       (Service - จัดการฐานข้อมูลสมาชิก)
```

-----

## 🌟 หลักการ OOP ที่สำคัญในโปรเจกต์นี้

นี่คือหัวใจของการออกแบบโปรเจกต์นี้:

### 1\. 📦 Encapsulation (การห่อหุ้ม)

  * **Why?** เพื่อซ่อนความซับซ้อนและป้องกันข้อมูล (Data Hiding)
  * **Example:**
      * `MoneyManager` ซ่อนตัวแปร `internalCash` (เงินทอนในตู้) ไว้เป็น `private` คลาสอื่นจะมาแก้ไขเงินในตู้โดยตรงไม่ได้
      * `InventoryManager` ซ่อน `Map<String, ItemSlot>` (คลังสินค้า) ไว้ คลาสอื่นต้องสั่งงานผ่านเมธอด `checkStock()`, `dispenseCart()` เท่านั้น
      * `AdminService` ห่อหุ้มเมธอดอันตราย (เช่น `collectCash()`) ไว้ ไม่ให้ `VendingMachine` (หน้าตู้) เรียกใช้ได้

### 2\. 🏛️ Inheritance & Abstraction (การสืบทอดและการเป็นนามธรรม)

  * **Why?** เพื่อลดความซ้ำซ้อน และสร้าง "พิมพ์เขียว" (Blueprint)
  * **Example:**
      * `Product` เป็น `abstract class` กำหนดว่าสินค้า "ต้องมี" อะไรบ้าง (เช่น `getPrice()`, `getName()`)
      * `Drink` และ `Snack` **สืบทอด (Inherit)** คุณสมบัติจาก `Product` ทำให้ทั้งคู่ "เป็น" (`IS-A`) Product แต่ก็มีรายละเอียดของตัวเอง (เช่น `volume`, `weight`)

### 3\. 🎨 Polymorphism (การมีหลายรูปแบบ)

  * **Why?** เพื่อความยืดหยุ่น โค้ดหลัก (Controller) ไม่ต้องผูกมัดกับวิธีใดวิธีหนึ่ง
  * **Example (Strategy Pattern):**
      * `MoneyManager` ไม่รู้ (และไม่จำเป็นต้องรู้) ว่าการรับเงินแบบ `QR` หรือ `Coin` ทำงานยังไง
      * มันแค่สร้าง Object จาก `PaymentMethod` (ซึ่งเป็น `Interface`)
      * เมื่อสั่ง `paymentMethod.receivePayment()` ตัว Java จะเลือกรันเมธอดที่ถูกต้องของคลาสลูก (เช่น `CoinReceiver`, `QRReceiver`) ให้เอง

### 4\. 🧩 Composition (การประกอบร่าง)

  * **Why?** เพื่อสร้าง Object ที่ซับซ้อนจาก Object ที่เล็กกว่า (หลักการ "Has-A")
  * **Example:**
      * `VendingMachineController` "มี" (`HAS-A`) `InventoryManager`, `MoneyManager`, และ `MemberDatabase`
      * `AdminService` "มี" (`HAS-A`) `InventoryManager` และ `MoneyManager`
      * วิธีนี้ยืดหยุ่นกว่าการใช้ Inheritance (เช่น `VendingMachineController` ไม่ควร *เป็น* `InventoryManager`)

-----

## 📖 Code Documentation (JavaDoc)

โปรเจกต์นี้ใช้ **JavaDoc** (คอมเมนต์แบบ `/** ... */`) เพื่ออธิบายโค้ด ซึ่งเป็นมาตรฐานการเขียนเอกสารใน Java

### ทำไมมันถึงสำคัญ?

1.  **อธิบาย "What" (ทำอะไร):** บอกว่าคลาสนี้, เมธอดนี้ หรือตัวแปรนี้ **มีไว้ทำไม**

      * เช่น `/** คลาสสำหรับ "โหมดแอดมิน" (Encapsulation) */`

2.  **อธิบาย "How" (ยังไง):** ใช้ Tags พิเศษเพื่ออธิบายรายละเอียด:

      * `@param`: อธิบายว่า Parameter แต่ละตัวคืออะไร (เช่น `@param slotCode รหัสช่อง A1, B2`)
      * `@return`: อธิบายว่าเมธอดนี้จะคืนค่าอะไรกลับไป
      * `@throws`: (สำคัญมาก) อธิบายว่าเมธอดนี้ **อาจโยน Error** อะไรออกมาได้บ้าง (เช่น `@throws OutOfStockException`)

เมื่อคุณใช้ JavaDoc ใน IDE (อย่าง IntelliJ หรือ Eclipse) เวลาคุณเรียกใช้เมธอดนั้นๆ มันจะขึ้นคำอธิบายเหล่านี้มาให้ทันที ทำให้คุณ (และเพื่อนร่วมทีม) ทำงานได้ง่ายขึ้นมาก โดยไม่ต้องเปิดเข้าไปอ่าน Logic ข้างในทุกครั้งครับ

-----

## ▶️ วิธีการรัน (How to Run)

1.  Clone โปรเจกต์นี้ลงในเครื่องของคุณ
2.  เปิดโปรเจกต์ด้วย IDE ที่คุณถนัด (เช่น IntelliJ IDEA, VS Code, หรือ Eclipse)
3.  ไปที่ไฟล์ `vendingmachine/VendingMachine.java`
4.  กดปุ่ม "Run"
5.  ใช้งานโปรแกรมผ่านหน้าต่าง Console (Terminal) ได้เลย
6.

Report : https://docs.google.com/document/d/1HKbb4TnQa1G5QExfCQk4326rQAjhPK5ad2Eu5Tft6L4/edit?usp=sharing

  <!-- เพิ่มเติม ✓ เลือกวิธีจ่ายผิด ให้ถามใหม่
              ✓ มี cancel ตอนจ่ายเงิน
              ✓ Have Delay dispensing items
              ✓ เช็ค varidation เบอร์ ตอนนี้มันเป็นstring
              ✓ จัด Format ใหม่
              ✓ ตอนนี้ เลือกได้แค่ไม่แบงค์ก็เหรียญ ถ้าคนจะใส่ทั้งคู่จะทำไม่ได้
              ทอนเงิน คำนวณรายละเอียดการทอน(เช่น เหรียญจะออกมา 5บาทกี่เหรียญ 10กี่ 1กี่)
              handle กรณีอ่านแบงค์ เหรียญไม่ออก return กลับมา
              ทอนเงินไม่พอ ยังไม่คืนเงินให้ return กลับมา

              แสดงว่าปัจจุบันใส่เงินเท่าไหร่แล้ว  -->
